# Team memory — littlepinkllama

Shared learnings for all roles. One line per entry, newest first. Per the memory-methodology skill.

- 2026-08-13: Site built as 74 static pages generated by `tools/gen.py` + `tools/gen_pages.py`; regeneration needs scraped Shopify data dir as argv[1] (live store `/products.json` is the catalog source). All order CTAs → WhatsApp `wa.me/919460074404` / Instagram `@little_pink_llama_`; no payment code permitted. Links must stay relative (GitHub Pages project path).
- 2026-09-21: Custom domain `littlepinkllama.com` now live (CNAME file added) — `gen.SITE_URL` updated to match, used by canonical/OG tags and `gen_sitemap.py`. Full SEO pass done: `head()` now emits meta description, canonical, OG/Twitter, and JSON-LD (`Organization`+`WebSite` home, `Product` on products, `BreadcrumbList` on collections, `FAQPage` on faq, `BlogPosting` on posts) for every page — never hand-add these per page, extend `head()`'s params instead. Added `tools/gen_404.py` and `tools/gen_sitemap.py` (previously ad-hoc/missing) plus `robots.txt`. All 74 pages verified: 0 broken links, 0 missing meta/canonical/H1, 0 images without alt, 0 invalid JSON-LD. Regenerate all 4 generators together after any change — they share `SITE_URL` and product data.
- 2026-09-21 (later): compared against the live Shopify original (uvdg81-wu.myshopify.com) — its product `<title>` tags lead with keywords ("Little Pink Llama Handmade Brooch for Kids – Cute Pink Llama Pin"), ours were generic ("Llama - Brooch | Little Pink Llama"). Rewrote all 53 product titles in `gen.py`'s `build_products()` to "{Name} Handmade {Type} for Kids | Little Pink Llama" (brand suffix dropped past 60 chars, same rule as blog titles). `product_type_noun(handle)` derives the type word from collection membership (`COLLECTION_NOUN`) for the 9 products whose name has no " - Type" suffix (crochet toys, some collar items) — Shopify's `product_type`/`tags` fields came back empty from the scrape, collection membership is the only type signal available. All 53 titles verified unique, ≤60 chars.
