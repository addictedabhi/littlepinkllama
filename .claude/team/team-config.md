# Team config — littlepinkllama

Generated by `/init-team`. This file + the overlays in `./overlays/` are the ONLY source of project facts for the virtual team. Commit this whole `.claude/team/` tree with the code.

## 1. Project
- **Name:** littlepinkllama
- **Description:** Static HTML/CSS/JS replica of littlepinkllama.com (Shopify store) with no payments — ordering via WhatsApp (+91 94600 74404) and Instagram DM; hosted on GitHub Pages.
- **Repo root marker:** this file lives at `.claude/team/team-config.md` relative to repo root. All paths below are repo-relative.

## 2. Mode & roster
- **Mode:** dev
- **Roster (dev):** technical-architect, developer, code-reviewer, tester
- **Orchestrator:** jarvis (always)
- Roster overrides (add/remove roles only with a documented reason): none

## 3. Protected branches & commits
- **Protected branch(es):** main
- **Feature branch naming:** feature/<short-description> (lowercase, hyphen-separated)
- **Commit convention:** Conventional Commits
- **Commit policy:** manual — agents never commit or push; the human reviews and commits.

## 4. Gates
Standard gates G1–G4 per the `team-protocol` skill. Project-specific ADDITIONAL gates (may add, never remove):
none

## 5. Document paths
- Requirements: docs/requirements/
- Design docs (dev mode): docs/design/
- Plans: docs/plans/
- ADRs (dev mode): docs/adr/

## 6. Memory paths
Per the `memory-methodology` skill. Default (recommended): `.claude/team/memory/`. Override: default

## 7. Open questions
none
